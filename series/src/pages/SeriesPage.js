import React from "react";
import { Text, View, StyleSheet } from "react-native";

const SeriesPage = ({ series }) => {
    return (
        <View>
            <Text>Essa é a SeriesMainPage</Text>
        </View>
    )
};

const styles = StyleSheet.create({

});

export default SeriesPage;